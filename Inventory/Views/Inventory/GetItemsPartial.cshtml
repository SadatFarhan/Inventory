@model List<Inventory.Models.Items>

<h3>Inventory Items</h3>

@if (Model != null && Model.Any())
{
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.ItemName: @item.Description</li>
        }
    </ul>
}
else
{
    <p>No items found for this inventory.</p>
}