@model Inventory.Models.AdminDashboard

@{
    ViewData["Title"] = "Admin Dashboard";
}

<h1>Admin Dashboard</h1>

<div class="row">
    <div class="col-md-6">
        <h2>Users</h2>
        <hr />
        @if (Model.Users != null && Model.Users.Any())
        {
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model.Users)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => user.UserName)</td>
                            <td>@Html.DisplayFor(modelItem => user.Email)</td>
                            <td>@Html.DisplayFor(modelItem => user.Role)</td>
                            <td>
                                <a asp-action="ChangeRole" asp-route-id="@user.Id">Change Role</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No users found.</p>
        }
    </div>

    <div class="col-md-6">
        <h2>Inventory Items</h2>
        <hr />
        @if (Model.InventoryItems != null && Model.InventoryItems.Any())
        {
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Name</th>
                      
                        <th>Quantity</th>
                        <th>Created By</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.InventoryItems)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.ItemName)</td>
                           
                            <td>@Html.DisplayFor(modelItem => item.Quantity)</td>
                            <td>@Html.DisplayFor(modelItem => item.Creator.UserName)</td>
                            <td>
                                <a asp-action="EditInventory" asp-route-id="@item.Id">Edit</a> |
                                <a asp-action="DeleteInventory" asp-route-id="@item.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No inventory items found.</p>
        }
    </div>
</div>